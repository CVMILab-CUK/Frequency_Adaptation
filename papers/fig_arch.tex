% Two panels: the system, and one StandAlone-attention site opened up. Drafted
% throughout -- one stroke weight for data flow, heavier for trained parts,
% flat tint for frozen blocks, square corners. Every label matches a symbol in
% models/attention_processor.py.
% <<AUTHOR-INPUT A38: 네트워크의 decoder skip tensor head 수(models/attention_processor.py 또는 ControlNet 인터페이스 기준)>>
\begin{figure*}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  font=\small, >=latex, line width=0.4pt,
  blk/.style   ={draw, minimum height=6mm, minimum width=13mm, inner xsep=2pt, align=center},
  frozen/.style={blk, fill=black!4, draw=black!50},
  train/.style ={blk, line width=0.9pt},
  lbl/.style   ={font=\scriptsize, inner sep=1.5pt},
  tiny/.style  ={font=\scriptsize, inner sep=1pt},
  flow/.style  ={->, line width=0.4pt},
  inj/.style   ={->, line width=1.1pt},
  minor/.style ={->, line width=0.55pt},
  skip/.style  ={densely dashed, line width=0.35pt},
  sum/.style   ={draw, circle, inner sep=0pt, minimum size=3.2mm, line width=0.9pt},
]

% ==================== (a) system ====================
\node[lbl, anchor=west] at (-0.4,1.55) {\textbf{(a)} condition path and the two injection sites};

\node[blk] (img)  at (0.5,0.85) {image $x$};
\node[blk] (gray) at (2.4,0.85) {gray};
\node[blk] (fft)  at (4.0,0.85) {FFT};
\node[draw, circle, minimum size=6.5mm, inner sep=0pt] (mask) at (5.5,0.85) {};
\fill[black!16] (mask.center) circle (1.6mm);
\node[blk] (ifft) at (7.1,0.85) {IFFT};
\node[train] (enc) at (9.3,0.85) {conv encoder};
\foreach \a/\b in {img/gray, gray/fft, fft/mask, mask/ifft, ifft/enc} \draw[flow] (\a)--(\b);
\node[tiny, below=0.6mm of mask] {radius $r$};
\node[lbl, below left=0.6mm and -4mm of enc, align=right] {no VAE};

\node[frozen] (e64) at (1.5,-0.6) {enc 64};
\node[frozen] (e32) at (1.5,-1.7) {enc 32};
\node[frozen] (e16) at (1.5,-2.8) {enc 16};
\node[frozen] (mid) at (4.1,-3.7) {mid 8};
\node[frozen] (d16) at (6.7,-2.8) {dec 16};
\node[frozen] (d32) at (6.7,-1.7) {dec 32};
\node[frozen] (d64) at (6.7,-0.6) {dec 64};
\foreach \a/\b in {e64/e32, e32/e16, d16/d32, d32/d64} \draw[flow] (\a)--(\b);
\draw[flow] (e16) |- (mid);  \draw[flow] (mid) -| (d16);
\node[lbl, left=1mm of e64, align=right] {frozen\\SD1.5};

\foreach \lv/\y in {64/-0.6, 32/-1.7, 16/-2.8}{
  \node[sum] (s\lv) at (4.1,\y) {\tiny$+$};
  \draw[skip] (e\lv)--(s\lv); \draw[skip,->] (s\lv)--(d\lv);}
\node[sum] (smid) at (5.4,-3.7) {\tiny$+$};

% path 1: skip residuals -- the dominant one
\draw[line width=1.1pt] (enc.south) -- (9.3,0.12) -- (4.1,0.12);
\foreach \n in {s64,s32,s16} \draw[inj] (4.1,0.12) -- (\n.north);
\draw[line width=1.1pt] (4.1,0.12) -- (4.1,-2.8);
\draw[inj] (9.3,-3.7) -- (smid.east);
\draw[line width=1.1pt] (9.3,0.12) -- (9.3,-5.15);
\node[lbl, right=1mm, align=left] at (9.45,-1.0)
  {\begin{minipage}{22mm}\raggedright zero-init $1{\times}1$ head\\per skip tensor\\[1pt]
   skip path (dominant)\end{minipage}};

% path 2: SA at the cross-attention sites -- the minor one
\node[train, minimum width=30mm] (sa) at (4.1,-5.15) {SA at 16 cross-attn sites};
\draw[minor] (9.3,-5.15) -- (sa.east);
\draw[minor] (sa.north) -- (mid.south);
\node[lbl, below=0.6mm of sa] {attention path \ (panel b)};
\node[frozen, minimum width=15mm] (clip) at (0.9,-5.15) {CLIP text};
\draw[flow] (clip) -- (sa.west);

% ==================== (b) one SA site ====================
\begin{scope}[xshift=12.2cm, yshift=0cm]
\node[lbl, anchor=west] at (-0.3,1.55) {\textbf{(b)} inside one site};
\draw[black!25, line width=0.35pt] (-0.45,-5.6) rectangle (5.6,1.3);

\node[blk, minimum width=17mm] (ip)  at (1.0,0.85) {condition};
\node[frozen, minimum width=17mm] (lat) at (4.1,0.85) {UNet latent};

\node[blk, minimum width=17mm] (dn)  at (1.0,-0.15) {$\downarrow$ to this res.};
\draw[flow] (ip) -- (dn);
\node[train, minimum width=24mm] (kv) at (1.0,-1.15) {$W_K,\,W_V$ ($1{\times}1$)};
\draw[flow] (dn) -- (kv);
\node[blk, minimum width=24mm] (uf) at (1.0,-2.15) {unfold $3{\times}3$};
\draw[flow] (kv) -- (uf);
\node[tiny, right=0.8mm of uf, align=left] {9 neighbours\\per position};

\node[frozen, minimum width=13mm] (q) at (4.1,-1.15) {$W_Q$ (frozen)};
\draw[flow] (lat) -- (q);
\node[tiny, right=0.6mm of q] {$Q$};

\node[blk, minimum width=30mm] (att) at (2.5,-3.1)
  {\small softmax$\!\left(QK^{\!\top}\!/\sqrt{d}\right)V$};
\draw[flow] (uf.south) |- ([yshift=3mm]att.north west) -- (att.north west);
\draw[flow] (q.south) |- ([yshift=3mm]att.north east) -- (att.north east);
\node[tiny, right=0.8mm of att] {over 9, not $L$};

\node[frozen, minimum width=17mm] (o) at (2.5,-4.0) {$W_O$ (frozen)};
\draw[flow] (att) -- (o);
\node[train, minimum width=17mm] (g) at (2.5,-4.85) {zero-init gate};
\draw[flow] (o) -- (g);
\node[tiny, right=0.8mm of g, align=left] {per channel};

\coordinate (outp) at (2.5,-5.35);
\draw[line width=0.9pt] (g) -- (outp);
\node[lbl, anchor=north, align=center] at (2.5,-5.4)
  {added to the latent \emph{and} to the layer output};
\end{scope}

\end{tikzpicture}}
\caption{\textbf{(a)} A disc of radius $r$ is masked out of the spectrum and a
trainable convolutional encoder reads the result at full resolution, never
through the VAE. Its features enter the frozen UNet twice: as zero-initialised
residuals on each decoder skip tensor ($\oplus$; three resolution levels and the
mid block are drawn, \AUTHORINPUT{A38} heads in the network), and through
StandAlone attention at
the 16 cross-attention sites. Stroke weight follows the measured split: at $r{=}0$, SC is
\pathSkipOnlyZero{} with only the skip path active and \pathAttnOnlyZero{} with
only attention (Sec.~\ref{sec:where}).
\textbf{(b)} One StandAlone site. The query comes from the UNet's own latent
through the frozen $W_Q$; keys and values come from the condition through
trained $1{\times}1$ projections, unfolded so each position attends over its
$3{\times}3$ neighbourhood rather than the whole map; a $1{\times}1$ window
performs at least as well (Sec.~\ref{sec:where}). The result passes the
frozen output projection and a zero-initialised per-channel gate, then is added
both into the latent --- so the text query below sees structure --- and to what
the layer returns.}
\label{fig:arch}
\end{figure*}
