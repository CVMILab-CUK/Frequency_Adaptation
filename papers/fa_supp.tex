% Supplementary material for the anonymous EG 2027 Full Papers submission.
% The prologue below is fa_eg.tex's, up to \input{numbers}, so the two PDFs
% cannot disagree on a macro or a style file.
% Frequency Adaptation -- Eurographics 2027 Full Papers submission.
%
% SRMv2 EG_2027 Instruction: use EGauthorGuidelines-conf-sub.tex. The prologue
% below follows that file, wrapped around the same fa_body.inc that fa_en.tex
% compiles, so the draft and the submission can never disagree on the text.
% Branch A: style files in egstyle_eg2027/ (unmodified EG_2027 package).
% Branch B (interim): conference-submission mode of egstyle/egpubl.cls.

\documentclass{egpubl}
\ConferenceSubmission % EG 2027 Full Papers (egstyle/egpubl.cls:1203)
\ConfYear{2027}       % footer "submitted to EUROGRAPHICS 2027." -- check against the EG_2027 sample PDF
% <<AUTHOR-INPUT A01: EG_2027 LaTeX 패키지 zip(SRMv2 EG_2027 Instruction의 LaTeX2e style file, fileID=20175, 로그인 필요)을 받아 egstyle_eg2027/에 풀어 둘 것 -- 없으면 conf-sub.tex의 실제 모드·연도 매크로와 submission 예시 PDF의 헤더·푸터를 대조할 수 없다>>

% !! *please* don't change anything above
% !! unless you REALLY know what you are doing
% ------------------------------------------------------------------------
\usepackage[T1]{fontenc}
\usepackage{dfadobe}

\usepackage{cite}
\BibtexOrBiblatex

\electronicVersion
\PrintedOrElectronic
\ifpdf \usepackage[pdftex]{graphicx} \pdfcompresslevel=9
\else \usepackage[dvips]{graphicx} \fi

\usepackage{egweblnk}
% end of prologue

% ---- what this paper adds on top of the template ------------------------
\usepackage{booktabs,amsmath,amssymb,xcolor}
\usepackage{placeins}
% fa_body.inc inputs the appendix itself unless this is defined, so the
% draft (fa_en.tex) keeps it in place while the submission prints it after
% the references.
\newcommand{\AppendixAfterReferences}{}
\usepackage{tikz}
\usetikzlibrary{positioning,calc}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\pgfplotsset{
  draft/.style={
    width=\columnwidth, height=41mm,
    axis lines=left, axis line style={line width=0.4pt},
    tick align=outside, tick style={line width=0.4pt, black},
    ymajorgrids, grid style={black!12, line width=0.3pt},
    label style={font=\scriptsize}, tick label style={font=\scriptsize},
    legend style={font=\scriptsize, draw=none, fill=none, cells={anchor=west},
                  inner sep=1pt},
    every axis plot/.append style={line width=0.7pt, mark size=1.7pt},
  }}
\setcounter{secnumdepth}{2}
\input{numbers}
\usepackage{xr}
\externaldocument{fa_eg}
% xr imports every label of the other document, including LastPage, which
% would fight this document's own page count in the footer. Drop it here,
% before \begin{document} reads the local .aux.
\makeatletter
\expandafter\let\csname r@LastPage\endcsname\@undefined
\makeatother
\renewcommand{\thetable}{S\arabic{table}}
% fa_appendix.inc uses the placeholder macro that fa_body.inc defines; this
% document does not input fa_body.inc, so it declares it the same way.
\providecommand{\AUTHORINPUT}[1]{\textbf{[AUTHOR-INPUT #1]}}

\title[Supplementary material]{Supplementary material for: A Frequency Dial for Structure-Conditioned Diffusion}
\author[SUBMISSION ID]{SUBMISSION ID}

\begin{document}
\maketitle
\input{fa_appendix.inc}
\end{document}
