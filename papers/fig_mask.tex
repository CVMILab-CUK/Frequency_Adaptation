% What the dial actually does, from radial_mask_numpy / high_pass_numpy in the
% repository, so the picture and the method cannot diverge.
\begin{figure*}[t]
\centering
\begin{tikzpicture}
\node[inner sep=0, anchor=south west] (m) at (0,0) {\includegraphics[width=\textwidth]{figures/mask}};
\foreach \lab [count=\i from 0] in {$r{=}0$, $0.05$, $0.1$, $0.2$, $0.3$, $0.5$, $0.7$, $1$}
  \node[font=\scriptsize, anchor=south, inner sep=1.5pt]
    at ($(m.north west)!{(\i*196+98)/1568}!(m.north east)$) {\lab};
\draw[black!40, line width=0.3pt] (m.north west) rectangle ++(0.125\textwidth,-0.125\textwidth);
\end{tikzpicture}
\caption{The dial, from the frequency side. Columns are
$r = 0,\,0.05,\,0.1,\,0.2,\,0.3,\,0.5,\,0.7,\,1.0$. \textbf{Row 1:} the radial
mask; black is removed. \textbf{Row 2:} the log-magnitude spectrum after
masking, with the hole growing outward. \textbf{Row 3:} the resulting
condition, exactly as the model receives it. \textbf{Row 4:} the same data
displayed at $\pm2\sigma$ so that the residual content is visible.
Above $r{\approx}0.2$ the condition looks like flat grey (averaged over
\energyN{} test images, its standard deviation falls from \condStdZero{} at
$r{=}0$ to \condStdOneHundred{} at $r{=}1$), yet the face
outline survives in row 4, and the model still reaches
SC \scMainTwoZero{} at $r{=}0.2$. The per-image normalisation leaves the model
only this low-contrast residue to read structure from.}
\label{fig:mask}
\end{figure*}
